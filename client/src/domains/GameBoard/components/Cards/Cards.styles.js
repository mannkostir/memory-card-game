import styled from 'styled-components';

export const CardsContainer = styled.div`
  display: flex;
  flex-flow: row wrap;
  gap: 1.5em;
  padding-top: 5em;
  justify-content: center;
  perspective: 1000px;
  transition: 1s ease;
`;
